export default function Home() {
  return (
    <span>
      This Project demos the way error boundaries interact with server components<br />
      The nav items lead to different error boundary setups that show their behavior in a server-side rendering context.
    </span>
  );
}
